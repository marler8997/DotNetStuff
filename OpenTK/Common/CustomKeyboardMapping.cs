using System;
using System.Collections.Generic;
/*
using Microsoft.Xna.Framework.Input;

namespace Marler.OpenTK.Common
{
    public class CustomKeyboardMapping
    {
        public readonly Dictionary<Keys, String> keyToName;
        public readonly Dictionary<String, Keys> nameToKey;

        public CustomKeyboardMapping()
        {
            this.keyToName = new Dictionary<Keys, String>();
            this.nameToKey = new Dictionary<String, Keys>();
        }
        public void Clear()
        {
            keyToName.Clear();
            nameToKey.Clear();
        }
        public void Add(Keys key, String name)
        {
            keyToName.Add(key, name);
            nameToKey.Add(name, key);
        }
        public void Set(Keys key, String name)
        {
            keyToName[key] = name;
            nameToKey[name] = key;
        }
    }
}
*/