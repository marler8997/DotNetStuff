using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;

namespace More
{
    /*
    public struct URatio32
    {
        public static readonly UnitRatio Zero = new UnitRatio(0, 1);
        public static readonly UnitRatio One = new UnitRatio(1, 0);

        UInt32 ratio32;        
        private URatio32(UInt32 ratio32)
        {
            this.ratio32 = ratio32;
        }


        public Single Multiply(Single value)
        {

        }
        public Double Multiply(Double value)
        {

        }




        public UnitRatio(Int32 numerator)
        {
            this.numerator = numerator;
            this.denominator = 1;
        }
        public Rational(Int32 numerator, UInt32 denominator)
        {
            this.numerator = numerator;
            this.denominator = denominator;

            if (numerator == 0)
            {
                if (denominator != 0)
                {
                    denominator = 1; // Normalize Zero
                }
            }
            else if (denominator == 0)
            {
                numerator = 1;       // Normalize Infinity
            }
        }
        public Boolean Equals(Rational r)
        {
            return this.numerator == r.numerator && this.denominator == r.denominator;
        }

    }
    */
}
